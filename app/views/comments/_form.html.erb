<%= form_for ([@post, @post.comments.build]) do |f| %>
	<%= f.label :name %><br>
	<%= f.text_field :name, :size => "60%x5%" %><br>
	<br>
	<%= f.label :body %><br>
	<%= f.text_area :body, :size => "60%x5%" %><br>
	<br>
	<%= f.submit %><br>
<% end %>